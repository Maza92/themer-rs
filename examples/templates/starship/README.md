# Starship Template

**Supported modes:** `replace`

## Example target

```toml
[[targets]]
name = "starship"
template = "starship.toml"
output = "~/.config/starship.toml"
mode = "replace"
reload_cmd = ""
```
