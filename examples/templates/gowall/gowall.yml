EnableImagePreviewing: false
themes:
  - name: "{{ name }}"
    colors:
      - "{{ base06 | hex_hash }}"
      - "{{ base07 | hex_hash }}"
      - "{{ purple | hex_hash }}"
      - "{{ dark_purple | hex_hash }}"
      - "{{ pink | hex_hash }}"
      - "{{ baby_pink | hex_hash }}"
      - "{{ orange | hex_hash }}"
      - "{{ yellow | hex_hash }}"
      - "{{ green | hex_hash }}"
      - "{{ teal | hex_hash }}"
      - "{{ base0B | hex_hash }}"
      - "{{ base0C | hex_hash }}"
      - "{{ blue | hex_hash }}"
      - "{{ white | hex_hash }}"
      - "{{ base06 | hex_hash }}"
      - "{{ base05 | hex_hash }}"
      - "{{ light_grey | hex_hash }}"
      - "{{ grey_fg2 | hex_hash }}"
      - "{{ grey_fg | hex_hash }}"
      - "{{ grey | hex_hash }}"
      - "{{ one_bg3 | hex_hash }}"
      - "{{ one_bg2 | hex_hash }}"
      - "{{ one_bg | hex_hash }}"
      - "{{ black2 | hex_hash }}"
      - "{{ black | hex_hash }}"
      - "{{ darker_black | hex_hash }}"
